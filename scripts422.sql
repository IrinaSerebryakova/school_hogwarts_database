CREATE TABLE mans (carId SERIAL PRIMARY KEY, name TEXT UNIQUE NOT NULL, age INTEGER CHECK (age > 0), driversLicense BOOLEAN DEFAULT 'false');
CREATE TABLE cars (carId SERIAL, manId SERIAL PRIMARY KEY, make TEXT NOT NULL, model TEXT NOT NULL, cost MONEY);